server:
  port: 10110  # 端口
spring:
  application:
    name: education-student-service #设置微服务名称
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.136.1:8848
      config:
        server-addr: 192.168.136.1:8848 #Nacos作为配置中心地址
        file-extension: yml #指定yml格式配置
#        group: PROD_GROUP   # 指定配置文件所在组
        group: DEV_GROUP   # 指定配置文件所在组
